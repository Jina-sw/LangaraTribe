
const Circles = () => {
    
    const icon = "";

    if(props.)

    return (
        <div id="course"
            onMouseEnter={() => props.setState("course")}
            onMouseLeave={() => props.setState("default")}
        ></div>
    );
}

export default Circles;